<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Client shop</title>
    <link href="~/Content/custom.css" rel="stylesheet" type="text/css" />
</head>
<body>
<div id="header">
    <div class="title">Shop store</div>
    <div class="login">
        Hello @User.Identity.Name |
        <a href="@Url.Action("Logout", "Shared")" class="link">logout</a> |
        <a href="@Url.Action("AssignedShops", "ShopAdministrationCore")" class="link">shops</a>
    </div>
</div>
    <div id="client-menu-left">
            @{Html.RenderAction("ShowMenuItems", "ShopAdminNav");}
    </div>
    <div id="content-shop-admin">
        @RenderBody()
    </div>
</body>
</html>
