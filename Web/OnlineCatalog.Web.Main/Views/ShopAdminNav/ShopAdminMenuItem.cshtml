@model OnlineCatalog.Web.Main.Models.MenuItemRouting[]

@foreach (var item in Model)
{
    @Html.RouteLink(item.MenuItem, new { controller = "ShopAdminNav", action = "RouteToAction", menuItemRouting = item.RouteTo})
}
